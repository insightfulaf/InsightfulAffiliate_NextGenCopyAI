# This file previously contained an accidentally committed SSH public key.
# The key material has been removed. If you need to use this key, store it in your ~/.ssh directory
# with an appropriate filename (for example, id_ed25519.pub) and do not track it in version control.
